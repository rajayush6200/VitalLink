window.APP_CONFIG = {
  // Before deploying the frontend, change this URL to your deployed Render backend URL.
  // Example: "https://vitallink-backend.onrender.com"
  API_BASE_URL: "http://localhost:5000"
};
